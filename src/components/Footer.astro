<footer>
    <p>Os vulcões oferecem um vislumbre do interior da Terra, mostrando poderosas forças geológicas por meio de erupções.</p>
</footer>

<style>
    footer {
        font-size: 1rem;
        position: fixed;
        left: 0;
        bottom: 0;
        width: 100%;
        min-height: 35px;
        padding: 0;
        padding-inline: 5%;
        display: flex;
        align-items: center;
        overflow: hidden;
        background-color: var(--background);
        border-top: 1px solid var(--border);
    }

    p {
        position: relative;
        width: 100%;
        white-space: nowrap;
        font-size: 0.9rem;
        color: var(--font);
        transform: translateX(100%);
        animation: scroll 35s linear infinite;
    }

    @keyframes scroll {
        0% {
            transform: translateX(100%);
        }
        100% {
            transform: translateX(-100%);
        }
    }
</style>